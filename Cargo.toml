[workspace]
members = [
  "contracts/common",
  "contracts/bulk_payment",
  "contracts/revenue_split",
  "contracts/cross_asset_payment",
  "contracts/vesting_escrow",
]
# Test-only upgrade target for vesting_escrow. It is built by hand into a
# committed .wasm fixture and must not be picked up by scaffold builds or the
# contract release workflow.
exclude = ["contracts/vesting_escrow/test_fixtures/upgraded_vesting"]
resolver = "2"

[workspace.package]
authors = ["The Aha Company"]
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/theahaco/scaffold-stellar"
version = "0.0.1"

[workspace.dependencies.soroban-sdk]
version = "23.4.0"

[workspace.dependencies.stellar-access]
git = "https://github.com/OpenZeppelin/stellar-contracts"
tag = "v0.6.0"

[workspace.dependencies.stellar-macros]
git = "https://github.com/OpenZeppelin/stellar-contracts"
tag = "v0.6.0"

[workspace.dependencies.stellar-tokens]
git = "https://github.com/OpenZeppelin/stellar-contracts"
tag = "v0.6.0"

[profile.release]
opt-level = "z"
debug = false
lto = true
debug-assertions = false
codegen-units = 1
panic = "abort"
overflow-checks = true
strip = true

[profile.release-with-logs]
debug-assertions = true
inherits = "release"
